---
phase: 01-canonical-receipt-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/test-flow.ts
  - README.md
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
user_setup: []
must_haves:
  truths:
    - Stamping the same `txSignature` twice returns the original receipt instead of writing a duplicate row.
    - `/api/v1/receipts` and `/api/v1/verify/:receiptId` expose the canonical Phase 1 contract rather than raw SQLite columns or premature `verified: true` semantics.
    - The repo has an automated smoke flow that proves the new request contract, idempotency, and honest unanchored verification state.
  artifacts:
    - apps/api/src/server.ts
    - apps/api/src/test-flow.ts
    - README.md
  key_links:
    - apps/api/src/server.ts uses the canonical row mapper for duplicate returns and receipt lists, and apps/api/src/test-flow.ts exercises those responses through a local Fastify server instance.
---

<objective>
Complete the canonical receipt lifecycle with idempotent persistence, canonical API responses, and a deterministic smoke harness.

Purpose: Phase 1 is not ready to execute until duplicate stamping, receipt listing, verifier output, and the repo's smoke flow all reflect the same honest contract.
Output: Idempotent stamp behavior, camelCase receipt serialization on read paths, and an inject-based smoke test that covers the end-to-end Phase 1 flow.
</objective>

<execution_context>
@/Users/kabdallreda/Desktop/lumen-layer/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kabdallreda/Desktop/lumen-layer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-receipt-pipeline/01-CONTEXT.md
@.planning/phases/01-canonical-receipt-pipeline/01-RESEARCH.md
@.planning/phases/01-canonical-receipt-pipeline/01-VALIDATION.md
@apps/api/src/server.ts
@apps/api/src/test-flow.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make stamping idempotent and serialize read paths through the canonical contract</name>
  <files>apps/api/src/server.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/receipt.ts, apps/api/src/verifier.ts, .planning/phases/01-canonical-receipt-pipeline/01-CONTEXT.md</read_first>
  <action>Update `POST /api/v1/stamp` so it first queries `receipts` by `tx_signature` and, when a row already exists, returns `200` plus the canonical camelCase receipt mapped through the shared row serializer per D-09. For new rows, only insert after the live bundle-status path succeeds. Update `GET /api/v1/receipts` to stop returning raw SQLite rows and instead map every row through the canonical serializer before replying with `{ receipts, count }`. Keep `GET /api/v1/verify/:receiptId` on the honest Phase 1 shape from Plan `01-01`, including `hashMatches` and `verified: false`, and add route response schemas in `server.ts` that reference the shared receipt contract where appropriate.</action>
  <verify>
    <automated>cd apps/api && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/server.ts` contains a `SELECT * FROM receipts WHERE tx_signature = ?` lookup before insert
    - `apps/api/src/server.ts` returns `200` for duplicate stamps and `201` for new stamps
    - `apps/api/src/server.ts` does not send raw `tx_signature` or `created_at` fields from `/api/v1/receipts`
  </acceptance_criteria>
  <done>Duplicate stamps are idempotent, read routes serialize camelCase receipts, and the API no longer leaks database field names.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the smoke flow to in-process injection and document the honest Phase 1 contract</name>
  <files>apps/api/src/server.ts, apps/api/src/test-flow.ts, README.md</files>
  <read_first>apps/api/src/server.ts, apps/api/src/test-flow.ts, README.md, .planning/phases/01-canonical-receipt-pipeline/01-VALIDATION.md, .planning/phases/01-canonical-receipt-pipeline/01-RESEARCH.md</read_first>
  <action>Refactor `apps/api/src/server.ts` to export `buildServer(deps?)` plus a `start()` wrapper that only listens when the module is executed directly, so tests can create the app without a background process. Rewrite `apps/api/src/test-flow.ts` to instantiate `buildServer()` and use `fastify.inject()` with a stubbed bundle lookup that returns `{ bundleId, slot, confirmationStatus, transactions: [txSignature] }`. The flow must assert that the first stamp uses only `txSignature`, `bundleId`, and optional `walletAddress`, the second identical stamp returns `200` with the same `receiptId`, the verify response returns `hashMatches: true` and `verified: false`, and the receipts list returns `createdAt` plus `walletAddress` instead of snake_case DB columns. Update `README.md` so the stamp contract no longer advertises caller-supplied `slot` or `confirmationStatus`, and note that Phase 1 receipts stay unverified until Phase 2 memo anchoring.</action>
  <verify>
    <automated>cd apps/api && npm run test:flow</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/test-flow.ts` contains `inject(` and does not contain `BASE_URL`
    - `apps/api/src/test-flow.ts` asserts a duplicate stamp returns the same `receiptId`
    - `README.md` does not describe `slot` or `confirmationStatus` as inputs to `POST /api/v1/stamp`
  </acceptance_criteria>
  <done>The repo has a deterministic automated smoke flow for the canonical receipt lifecycle and the public docs match the new Phase 1 contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/api && npm run build`
- [ ] `cd apps/api && npm run test:flow`
- [ ] Duplicate stamping, receipt listing, and verification output all reflect the canonical Phase 1 contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 execution will have an automated proof that live stamping, idempotency, and canonical read-path serialization work together
  </success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-receipt-pipeline/01-03-SUMMARY.md`
</output>
