---
phase: 01-canonical-receipt-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/receipt.ts
  - apps/api/src/verifier.ts
  - packages/schema/receipt-schema.json
autonomous: true
requirements:
  - PIPE-03
user_setup: []
must_haves:
  truths:
    - Receipt objects returned from backend helpers use one canonical camelCase shape with `createdAt`, nullable `onChainMemo`, and `verified: false`.
    - Hash integrity is represented separately from final verification so Phase 1 does not claim on-chain proof before memo anchoring exists.
  artifacts:
    - apps/api/src/receipt.ts
    - apps/api/src/verifier.ts
    - packages/schema/receipt-schema.json
  key_links:
    - apps/api/src/receipt.ts defines the canonical receipt fields and hash helper that packages/schema/receipt-schema.json documents and apps/api/src/verifier.ts consumes.
---

<objective>
Establish the canonical receipt contract and the honest Phase 1 verification semantics before any live Jito wiring happens.

Purpose: Phase 1 cannot safely use live bundle data until the backend, DB serialization, and shared schema all describe the same receipt shape.
Output: A single canonical receipt model, a real shared schema file, and a verifier that separates `hashMatches` from `verified`.
</objective>

<execution_context>
@/Users/kabdallreda/Desktop/lumen-layer/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kabdallreda/Desktop/lumen-layer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-receipt-pipeline/01-CONTEXT.md
@.planning/phases/01-canonical-receipt-pipeline/01-RESEARCH.md
@.planning/phases/01-canonical-receipt-pipeline/01-VALIDATION.md
@README.md
@apps/api/src/receipt.ts
@apps/api/src/verifier.ts
@packages/schema/receipt-schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the shared receipt contract to the canonical Phase 1 shape</name>
  <files>packages/schema/receipt-schema.json, apps/api/src/receipt.ts</files>
  <read_first>packages/schema/receipt-schema.json, apps/api/src/receipt.ts, README.md, .planning/phases/01-canonical-receipt-pipeline/01-CONTEXT.md</read_first>
  <action>Replace the custom `fields` object in `packages/schema/receipt-schema.json` with a JSON Schema Draft 7 object schema whose required properties are exactly `receiptId`, `txSignature`, `bundleId`, `slot`, `confirmationStatus`, `receiptHash`, `onChainMemo`, `attestationLevel`, `walletAddress`, `verified`, and `createdAt`. Set `additionalProperties: false`, type `createdAt` as an integer, and type `onChainMemo` plus `walletAddress` as `['string', 'null']`. In `apps/api/src/receipt.ts`, make `LumenReceipt` match those fields exactly, keep `computeReceiptHash(txSignature, bundleId, slot)` as the only canonical hash helper, and update `buildReceipt` so it returns `verified: false`, `onChainMemo: null`, and `createdAt: Date.now()` per D-03, D-04, D-05, and D-06. Do not leave `timestamp` anywhere in the shared contract.</action>
  <verify>
    <automated>cd apps/api && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/schema/receipt-schema.json` contains `"createdAt"` and does not contain `"timestamp"`
    - `packages/schema/receipt-schema.json` contains `"walletAddress"` and `"type": ["string", "null"]`
    - `apps/api/src/receipt.ts` contains `verified: false`
  </acceptance_criteria>
  <done>The schema file and runtime receipt type expose the same Phase 1 field set and `buildReceipt` no longer marks receipts as verified.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize row serialization and honest verification semantics</name>
  <files>apps/api/src/receipt.ts, apps/api/src/verifier.ts</files>
  <read_first>apps/api/src/receipt.ts, apps/api/src/verifier.ts, .planning/phases/01-canonical-receipt-pipeline/01-CONTEXT.md, .planning/phases/01-canonical-receipt-pipeline/01-RESEARCH.md</read_first>
  <action>Add explicit helpers in `apps/api/src/receipt.ts` to translate SQLite receipt rows with columns `id`, `tx_signature`, `bundle_id`, `confirmation_status`, `receipt_hash`, `on_chain_memo`, `attestation_level`, `wallet_address`, `verified`, and `created_at` into the canonical camelCase receipt shape. Update `apps/api/src/verifier.ts` to reuse that mapping, recompute the hash through `computeReceiptHash`, and expose `hashMatches` as the integrity result while keeping `verified` false until Phase 2 memo anchoring exists per D-06. Only populate `error` when the recomputed hash disagrees with the stored hash; a matching hash alone must not flip `verified` to true.</action>
  <verify>
    <automated>cd apps/api && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/verifier.ts` contains `hashMatches`
    - `apps/api/src/verifier.ts` does not set `verified` from `recomputedHash === receipt.receipt_hash`
    - `apps/api/src/receipt.ts` contains a helper that maps `created_at` to `createdAt`
  </acceptance_criteria>
  <done>The verifier and any future route serializers can consume one canonical row-to-receipt mapper without implying full verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/api && npm run build`
- [ ] `packages/schema/receipt-schema.json` is a canonical Phase 1 receipt contract
- [ ] `apps/api/src/verifier.ts` exposes integrity without claiming final verification
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `PIPE-03` is satisfied by one receipt contract across runtime helpers, verifier output, and the shared schema file
  </success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-receipt-pipeline/01-01-SUMMARY.md`
</output>
