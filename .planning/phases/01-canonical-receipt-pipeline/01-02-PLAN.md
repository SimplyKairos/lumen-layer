---
phase: 01-canonical-receipt-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/api/src/bam-service.ts
  - apps/api/src/stamp-service.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
user_setup: []
must_haves:
  truths:
    - `POST /api/v1/stamp` accepts only `txSignature`, `bundleId`, and optional `walletAddress`, then fills receipt bundle fields from Jito data.
    - No receipt row is created when Jito has no landed bundle status, returns incomplete data, or the submitted transaction signature is not part of the bundle.
  artifacts:
    - apps/api/src/bam-service.ts
    - apps/api/src/stamp-service.ts
    - apps/api/src/server.ts
  key_links:
    - apps/api/src/server.ts validates the request body, calls apps/api/src/stamp-service.ts, and the service uses apps/api/src/bam-service.ts to fetch live bundle status before building a receipt.
---

<objective>
Replace placeholder stamp inputs with a truthful live bundle-status pipeline.

Purpose: Phase 1 only becomes trustworthy when the route stops accepting client-supplied slot/status metadata and binds receipts to real Jito bundle data.
Output: A normalized Jito lookup helper, a stamp service that classifies failures honestly, and a route schema that only accepts the canonical input contract.
</objective>

<execution_context>
@/Users/kabdallreda/Desktop/lumen-layer/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kabdallreda/Desktop/lumen-layer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-receipt-pipeline/01-CONTEXT.md
@.planning/phases/01-canonical-receipt-pipeline/01-RESEARCH.md
@apps/api/src/bam-service.ts
@apps/api/src/server.ts
@apps/api/src/receipt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize Jito bundle-status lookup around the official endpoint</name>
  <files>apps/api/src/bam-service.ts, apps/api/src/stamp-service.ts</files>
  <read_first>apps/api/src/bam-service.ts, .planning/phases/01-canonical-receipt-pipeline/01-RESEARCH.md, .planning/phases/01-canonical-receipt-pipeline/01-CONTEXT.md</read_first>
  <action>Update `apps/api/src/bam-service.ts` so `getBundleData` posts to `${JITO_URL}/api/v1/getBundleStatuses` with `method: 'getBundleStatuses'` and `params: [[bundleId]]`, matching the official Jito docs instead of reusing `/api/v1/bundles`. Normalize both `confirmation_status` and `confirmationStatus`, and surface a structured result that distinguishes `ok`, `not_ready`, and `lookup_failed` states. Treat missing `value[0]`, missing `slot`, missing confirmation status, missing transactions, or any returned `err` object as non-success paths. Create `apps/api/src/stamp-service.ts` to consume that lookup result and produce a typed stamp outcome rather than forcing `server.ts` to decode raw Jito responses.</action>
  <verify>
    <automated>cd apps/api && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/bam-service.ts` contains `/api/v1/getBundleStatuses`
    - `apps/api/src/bam-service.ts` contains `confirmation_status ??` or equivalent normalization
    - `apps/api/src/stamp-service.ts` exists and contains a typed success/failure result for stamping
  </acceptance_criteria>
  <done>The codebase has one normalized Jito lookup seam and one stamp service that can classify "not ready" vs. "usable" bundle data without leaking raw JSON-RPC parsing into the route.</done>
</task>

<task type="auto">
  <name>Task 2: Shrink the stamp request contract and classify failures honestly</name>
  <files>apps/api/src/server.ts, apps/api/src/stamp-service.ts</files>
  <read_first>apps/api/src/server.ts, apps/api/src/stamp-service.ts, apps/api/src/receipt.ts, .planning/phases/01-canonical-receipt-pipeline/01-CONTEXT.md</read_first>
  <action>Update `POST /api/v1/stamp` in `apps/api/src/server.ts` to use a Fastify `schema.body` that requires only `txSignature` and `bundleId`, permits optional `walletAddress`, and sets `additionalProperties: false` per D-01. Stop reading `slot` and `confirmationStatus` from `request.body`. Call the stamp service to fetch live bundle status, reject the request when the returned `transactions` array does not include the submitted `txSignature`, and only build a receipt from live `bundleId`, `slot`, and `confirmationStatus` per D-02. Return `503 { error: 'bundle_status_unavailable', retryable: true }` for `not_ready` or retryable lookup failures per D-07 and D-08, and return `422 { error: 'tx_signature_not_in_bundle', retryable: false }` when the bundle exists but does not contain the claimed transaction signature.</action>
  <verify>
    <automated>cd apps/api && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/server.ts` no longer destructures `slot` or `confirmationStatus` from `request.body`
    - `apps/api/src/server.ts` contains a Fastify body schema with required `txSignature` and `bundleId`
    - `apps/api/src/stamp-service.ts` checks that the bundle transactions include the submitted signature
  </acceptance_criteria>
  <done>The public stamp route only accepts the canonical request body, obtains live bundle fields from Jito, and fails clearly instead of creating partial or misleading receipts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd apps/api && npm run build`
- [ ] `POST /api/v1/stamp` only accepts the locked Phase 1 request shape
- [ ] The live bundle lookup path distinguishes retryable upstream failure from semantic mismatch
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `PIPE-01` and `PIPE-02` are satisfied by a truthful stamp pipeline that depends on live bundle metadata
  </success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-receipt-pipeline/01-02-SUMMARY.md`
</output>
