{
  "version": "0.1.0",
  "name": "LumenReceipt",
  "description": "Canonical execution fairness receipt for Jito bundle transactions on Solana",
  "fields": {
    "receiptId": "string",
    "txSignature": "string",
    "bundleId": "string",
    "slot": "number",
    "confirmationStatus": "string",
    "receiptHash": "string",
    "onChainMemo": "string",
    "timestamp": "number",
    "verified": "boolean",
    "attestationLevel": "string"
  },
  "attestationLevels": {
    "BUNDLE_VERIFIED": "Transaction confirmed via getBundleStatuses — bundle ID, slot, and confirmation status on-chain",
    "BAM_ATTESTED": "Full TEE attestation digest bound to receipt — pending BAM API access"
  }
}